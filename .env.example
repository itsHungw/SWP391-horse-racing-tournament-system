# Copy to .env and adjust. .env is gitignored — never commit real secrets.
# Used by docker-compose.yml and when running the backend locally.

# --- Spring profile ---
# dev  -> local SQL Server + MinIO (this file's defaults)
# prod -> real cloud DB + real AWS S3 (IAM role)
SPRING_PROFILES_ACTIVE=dev

# --- Database (local SQL Server from docker-compose) ---
DB_SA_PASSWORD=Local_Dev_Password123
DB_NAME=horseracing
# Host port 14330 avoids clashing with a locally installed SQL Server on 1433.
DB_URL=jdbc:sqlserver://localhost:14330;databaseName=horseracing;encrypt=true;trustServerCertificate=true
DB_USERNAME=sa
DB_PASSWORD=Local_Dev_Password123

# --- Auth ---
AUTH_JWT_SECRET=change-me-to-a-long-random-secret-at-least-32-characters

# --- Object storage ---
# Local (MinIO): keep the endpoint + static keys below.
# Real AWS: set AWS_S3_BUCKET to the cloud bucket, leave AWS_S3_ENDPOINT,
# AWS_S3_ACCESS_KEY, AWS_S3_SECRET_KEY blank, and rely on the instance IAM role.
AWS_S3_BUCKET=horseracing-dev
AWS_REGION=ap-southeast-1
AWS_S3_ENDPOINT=http://localhost:9000
AWS_S3_ACCESS_KEY=minioadmin
AWS_S3_SECRET_KEY=minioadmin

# --- Mail (optional locally) ---
APP_MAIL_ENABLED=false

# --- VNPay (ví điện tử / nạp tiền) ---
# Đăng ký merchant sandbox tại https://sandbox.vnpayment.vn để lấy TmnCode + HashSecret.
# Để trống thì tính năng nạp tiền bị tắt (báo SERVICE_UNAVAILABLE).
VNPAY_TMN_CODE=
VNPAY_HASH_SECRET=
VNPAY_PAY_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
VNPAY_RETURN_URL=http://localhost:8080/api/v1/wallet/vnpay/return
VNPAY_FRONTEND_RETURN_URL=http://localhost:5173/wallet
VNPAY_MIN_AMOUNT=10000
VNPAY_MAX_AMOUNT=50000000
