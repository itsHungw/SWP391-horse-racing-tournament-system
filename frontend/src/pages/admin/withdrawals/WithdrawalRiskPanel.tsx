import { AlertTriangle, CheckCircle2, Info, ShieldAlert } from "lucide-react";
import type { WithdrawalRiskAssessment } from "../../../types/wallet";
import { riskPresentation } from "./withdrawalViewModel";

export function WithdrawalRiskPanel({ risk }: { risk: WithdrawalRiskAssessment }) {
  const presentation = riskPresentation[risk.level];
  return <section aria-labelledby="risk-assessment-heading" className="border border-slate-200 bg-[#fafaf8] p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] font-black uppercase tracking-[0.2em] text-slate-400">Rule-based assessment</p><h3 id="risk-assessment-heading" className="mt-1 text-lg font-black text-[#070f4f]">Risk evidence</h3></div><span className={`inline-flex items-center gap-2 border px-3 py-1.5 text-xs font-black uppercase ${presentation.className}`}>{risk.level === "HIGH" ? <ShieldAlert className="h-4 w-4" /> : <CheckCircle2 className="h-4 w-4" />}{presentation.label}</span></div>{risk.findings.length === 0 ? <p className="mt-5 flex items-center gap-2 text-sm font-semibold text-emerald-800"><CheckCircle2 className="h-4 w-4" /> No configured risk rule was triggered.</p> : <div className="mt-4 space-y-3">{risk.findings.map((finding) => <article key={finding.code} className="border-l-4 border-rose-500 bg-white p-4"><div className="flex gap-3"><AlertTriangle className="mt-0.5 h-5 w-5 shrink-0 text-rose-600" /><div><h4 className="font-black text-slate-900">{finding.title}</h4><p className="mt-1 text-sm leading-6 text-slate-600">{finding.explanation}</p><dl className="mt-3 grid gap-2 text-xs sm:grid-cols-2"><div><dt className="font-black uppercase tracking-wider text-slate-400">Evidence</dt><dd className="mt-1 font-bold text-slate-800">{finding.evidence}</dd></div><div><dt className="font-black uppercase tracking-wider text-slate-400">Suggested check</dt><dd className="mt-1 font-bold text-slate-800">{finding.suggestedCheck}</dd></div></dl></div></div></article>)}</div>}{risk.contextMarkers.length ? <p className="mt-4 flex items-center gap-2 text-xs text-slate-500"><Info className="h-4 w-4" /> Context: {risk.contextMarkers.map((marker) => marker.replaceAll("_", " ").toLowerCase()).join(", ")}</p> : null}</section>;
}
